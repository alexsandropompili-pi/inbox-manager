# Normalize all text files to LF on commit (Vercel / CI run on Linux).
* text=auto eol=lf

# Binaries — no conversion.
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
*.ico  binary
*.woff binary
*.woff2 binary
